# Paulo Vasconcelos
# paulobvasconcelos@gmail.com
# 2019 july

class Bike:
	
	def __init__(self, brand, model, image, price, sizes, colors, frame, fork, rearShock, stem, handlebars, seatpost, saddle, brakes, brakeLevers, shifter, frontDerailleur, rearDerailleur, cassette, chain, crank, bottomBracket, wheels, hubs, spokes, rims, tires, wheelSize, suspensionType, drivetrain, brakeType, frameMaterial):
		self.brand = brand
		self.model = model
		self.image = image
		self.price = price
		self.sizes = sizes
		self.colors = colors
		self.frame = frame
		self.fork = fork
		self.rearShock = rearShock
		self.stem = stem
		self.handlebars = handlebars
		self.seatpost = seatpost
		self.saddle = saddle
		self.brakes = brakes
		self.brakeLevers = brakeLevers
		self.shifter = shifter
		self.frontDerailleur = frontDerailleur
		self.rearDerailleur = rearDerailleur
		self.cassette = cassette
		self.chain = chain
		self.crank = crank
		self.bottomBracket = bottomBracket
		self.wheels = wheels
		self.hubs = hubs
		self.spokes = spokes
		self.rims = rims
		self.tires = tires
		self.wheelSize = wheelSize
		self.suspensionType = suspensionType
		self.drivetrain = drivetrain
		self.brakeType = brakeType
		self.frameMaterial = frameMaterial